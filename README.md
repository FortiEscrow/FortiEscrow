# FortiEscrow
FortiEscrow is a smart contract-based escrow framework in the Tezos ecosystem, designed with a security-first and correctness-first approach. FortiEscrow is not an application, but a framework (primitive) that can be reused by many applications.
